# Isi salah satu saja (Supabase ATAU API custom) sesuai backend yang dipakai.
# Kalau dua-duanya kosong, leaderboard otomatis pakai data dummy di
# lib/leaderboard-data.ts — halaman tetap jalan normal.

# --- Opsi 1: Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Opsional, dipakai di server kalau tabel diproteksi RLS dan butuh akses penuh
SUPABASE_SERVICE_ROLE_KEY=

# --- Opsi 2: REST API custom ---
# Harus mengembalikan JSON array yang match dengan interface LeaderboardEntry
# di lib/leaderboard-data.ts
LEADERBOARD_API_URL=
